Extract the zip file and copy the Quran.sql on this folder, the same location as the batch file.

Run the batch file.

Thee database is now ready.


To make Language filter functional run the LanguageFilterScript.sql into the databasse.

This script will add a new table 'Langrages' and add a column 'LanguageID' to the Translators table and all other necessary changes.
